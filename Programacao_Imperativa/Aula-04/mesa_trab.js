{
    let nome = "José da Silva";
    let idade = 27;
    let peso = 83.5;
    let altura = 1.70;
    let plano = "Ouro";
    let imc = peso / (altura * altura);
    console.log(nome + " tem " + idade + " anos e seu índice de massa corporal (IMC) é de " + Math.round(imc));
};

{
    let nome = "Carlos de Souza";
    let idade = 28;
    let peso = 80.1;
    let altura = 1.76;
    let plano = "Diamante";
    let imc = peso / (altura * altura);
    console.log(nome + " tem " + idade + " anos e seu índice de massa corporal (IMC) é de " + Math.round(imc));
};

{
    let nome = "Aline Ferreira";
    let idade = 33;
    let peso = 63.7;
    let altura = 1.53;
    let plano = "Não Possui";
    let imc = peso / (altura * altura);
    console.log(nome + " tem " + idade + " anos e seu índice de massa corporal (IMC) é de " + Math.round(imc));
};

{
    let nome = "Ana Paula";
    let idade = 26;
    let peso = 55.0;
    let altura = 1.62;
    let plano = "Prata";
    let imc = peso / (altura * altura);
    console.log(nome + " tem " + idade + " anos e seu índice de massa corporal (IMC) é de " + Math.round(imc));
};
